# Testing Foundation

Vitest is configured for packages and services. Playwright is installed at the workspace root for end-to-end suites that will be added during Wave 1.

Shared deterministic fixtures and small async utilities live in `packages/testing`.
